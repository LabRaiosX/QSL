
Compilation and installation:

Make sure you have installed the
Qt development libraries and CMake.
after extracting qsl, open a terminal
in the qsl directory then run the following
commands

$ mkdir build
$ cd build
$ cmake ..
$ make
$ sudo make install
